#include <iostream>





class Newton_raphson
{
    public:
        double eps;
        unsigned long max_iter;
        unsigned long iter_time;

        Newton_raphson(double& eps, unsigned long & max_iter) : eps(0.001), max_iter(10000), iter_time(0)
        {
            while (/* condition */)
            {
                /* code */
            }
            
        } 

}_;